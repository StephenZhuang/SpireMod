- 默认Mac Steam路径下可直接运行`./scripts/build-mod.sh`生成`SpireMod.jar`到`SlayTheSpire.app/Contents/Resources/mods/`
- 支持通过环境变量覆盖关键路径：`STS_JAR`、`MTS_JAR`、`MODS_DIR`
- Gradle构建需确保本地存在对应jar并通过`stsJar`/`mtsJar`/`modsDir`属性或默认路径解析