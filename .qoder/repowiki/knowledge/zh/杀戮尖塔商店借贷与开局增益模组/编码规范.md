- 使用静态工具类封装跨补丁共享的状态与规则（如`LoanState`提供常量、校验与变更方法）
- 通过`SpirePatch`对目标类方法进行Postfix注入，并在需要时嵌套Update/Render子补丁以分离生命周期逻辑
- UI交互采用Hitbox hover/click状态机驱动，并结合启用条件控制按钮可见性与可用性
- 关键操作前后同步`player.displayGold`与真实金币，确保界面数值一致性